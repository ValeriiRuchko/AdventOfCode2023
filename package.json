{
  "scripts": {
    "dev-watch": "tsc --watch & node --watch ./dist/$FILE",
    "debug": "tsc && node --inspect-brk ./dist/$FILE",
    "test": "vitest"
  },
  "type": "module",
  "dependencies": {
    "@types/node": "^20.10.1"
  },
  "devDependencies": {
    "vitest": "^0.34.6"
  }
}
